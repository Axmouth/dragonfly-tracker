import { AddCsrfHeaderInterceptor } from './add-csrf-header-interceptor';

describe('AddCsrfHeaderInterceptor', () => {
  it('should create an instance', () => {
    expect(new AddCsrfHeaderInterceptor()).toBeTruthy();
  });
});
