import { AuthFailedResponse } from './auth-failed-response';

describe('AuthFailedResponse', () => {
  it('should create an instance', () => {
    expect(new AuthFailedResponse()).toBeTruthy();
  });
});
