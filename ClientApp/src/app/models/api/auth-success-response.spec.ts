import { AuthSuccessResponse } from './auth-success-response';

describe('AuthSuccessResponse', () => {
  it('should create an instance', () => {
    expect(new AuthSuccessResponse()).toBeTruthy();
  });
});
